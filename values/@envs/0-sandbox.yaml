# ------------------------------------------------------------------
# yaml-language-server: $schema=../../chart/values.schema.json
# title               : Sandbox env.
# tags                : [ chart ]
# categories          : [ k8s, helm ]
# ------------------------------------------------------------------

Config:
  autoupdate:
    enabled: true
    paused: false
  extend:
    default: default
    image: dev
    power: min
    region: skip

global:
  some: false
