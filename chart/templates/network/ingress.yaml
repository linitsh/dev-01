{{- if not .Values.Settings.debug -}}
{{- if .Values.Network.include    -}}
{{- $i := .Values.Network.Ingress -}}
{{- if $i.enabled                 -}}

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "_.names.fullname" . }}
  labels:
    {{- include "_.labels.standard" . | nindent 4 }}
  {{- with $i.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with $i.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if $i.tls }}
  tls:
    {{- range $i.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range $i.Hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "_.names.fullname" $ }}
                port:
                  number: {{ $.Values.Network.Service.port }}
          {{- end }}
    {{- end }}

{{- end }}
{{- end }}
{{- end }}
