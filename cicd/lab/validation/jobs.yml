

validate_1:
  stage: validation
  image: oven/bun:slim
  script:
    - printenv
    - bun cicd/lab/validation/scripts/chart.ts

validate_chart:
  stage: validation
  image: oven/bun:slim
  script:
    - printenv
    - pwd
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
      when: always
    - when: never

