variables:
  BUILD_SCRIPTS: cicd/lab/build/scripts
build_1:
  stage: build
  image: ubuntu:20.04
  script:
    - printenv
    - bash $BUILD_SCRIPTS/script.sh
  # rules:
  #   - if: $CI_PIPELINE_SOURCE == 'web'
  #     when: always
  #   - when: never

