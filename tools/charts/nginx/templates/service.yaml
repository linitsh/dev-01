apiVersion: v1
kind: Service
metadata:
  name: {{ include "silver.fullname" . }}
  labels:
    {{- include "silver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "silver.selectorLabels" . | nindent 4 }}
